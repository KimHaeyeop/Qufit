import { useEffect, useState } from 'react';

export interface TimerProps {
    endSec: number;
    afterFunc: () => void;
}
const useMember = () => {
    return restSec;
};

export default useMember;
